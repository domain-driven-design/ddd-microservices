spring:
  datasource:
    url: jdbc:mysql://localhost:3308/ddd-microservice-demo?useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    baseline-on-migrate: true

liteflow:
  enable: true
  rule-source: config/flow.xml
